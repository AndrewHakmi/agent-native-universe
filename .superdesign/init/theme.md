# Theme inventory

## Compact token summary

No CSS, Tailwind configuration, theme provider, font, color, spacing, radius, shadow, or breakpoint tokens exist in the repository at initialization time.

The new target must source its visual rules from `.superdesign/design-system.md`. It should use CSS custom properties and a dependency-free stylesheet suitable for a static Observer UI served by Node.js.

## Raw source dumps

No theme source files exist yet.
