# Page dependency trees

No rendered pages exist in the repository at initialization time. The Observer UI is a new target, so there is no representative existing page and no dependency tree to reproduce.
