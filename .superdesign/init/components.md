# Shared UI components

No frontend framework or shared UI primitives exist in the repository at initialization time. The package is a dependency-free Node.js/TypeScript runtime. The new Observer UI must therefore introduce its own small, reusable primitives without fabricating compatibility with an existing component library.
