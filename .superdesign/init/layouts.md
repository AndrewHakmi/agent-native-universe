# Shared layouts

No rendered application shell, navigation, sidebar, header, or footer exists in the repository at initialization time. The Observer UI is a new target. Its shell should remain read-only and must not expose controls that mutate an experiment.
